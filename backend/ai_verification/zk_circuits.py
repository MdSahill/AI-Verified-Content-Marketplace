
"""Utilities that would interact with ZK circuits (placeholders)."""

def compile_circuit(path: str) -> str:
    """Pretend to compile a circom file and return compiled artifact path."""
    return path + ".r1cs"
